{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -d webapp ]; then cd webapp; fi; npm install",
  "buildCommand": "if [ -d webapp ]; then cd webapp; fi; npm run build",
  "devCommand": "if [ -d webapp ]; then cd webapp; fi; npm run dev"
}
